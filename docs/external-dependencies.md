# External dependencies and what they unblock

Inputs only the project owner can provide. Check here before starting a phase; work around a missing input where noted rather than blocking.

| Input                                                                                                                                                            | Unblocks                                                                                                                                         | Status (2026-09-21)                                                                          | Workaround                                                                                                                                                                                                                      |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Google Maps API key (Maps JavaScript, Places New, Geocoding) + Map ID → `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`, `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID` in both `.env.local` | Map canvas + markers on `/mapas` and `/negocios/[id]`; Places autocomplete for branch addresses                                                  | **In place** (2026-09-22): key, Map ID, Places API (New) enabled; autocomplete verified live | Restrict the key to HTTP referrers (`localhost:3000`, `localhost:3001`, the future domains). Without the key the map is a placeholder and the address field a plain input; manual coordinates / "Usar mi ubicación" always work |
| Google OAuth client configured in Supabase Auth (same Google Cloud project)                                                                                      | "Continuar con Google" on people-web                                                                                                             | Missing                                                                                      | Email/password works                                                                                                                                                                                                            |
| Wompi sandbox keys (`pub_test_`, `prv_test_`, `test_events_`, `test_integrity_`) + subscription price in COP                                                     | Phase 6 card capture, trial gate, billing cron, webhook                                                                                          | **In place** (2026-09-22, price 20 000 COP)                                                  | — Production keys wait for Phase 10                                                                                                                                                                                             |
| `SUPABASE_SECRET_KEY` pasted into `apps/people-web/.env.local` by the owner                                                                                      | Any server code using `createAdminSupabase()` (billing, webhooks, push dispatch)                                                                 | In place (2026-09-22)                                                                        | —                                                                                                                                                                                                                               |
| Vault secrets `people_web_url` + `billing_cron_secret` (SQL editor; the second must equal `BILLING_CRON_SECRET` in `apps/people-web/.env.local`)                 | The hourly `billing-run-hourly` cron job (scheduled, currently a no-op)                                                                          | Missing (needs a public URL first)                                                           | Trigger `/api/billing/run` by hand with the header (see verification-playbook)                                                                                                                                                  |
| Wompi "URL de eventos" pointing at `<public URL>/api/wompi/webhook` (Wompi dashboard → Desarrolladores)                                                          | Real-time payment outcomes                                                                                                                       | Not configured                                                                               | The billing run reconciles pending payments from Wompi every hour, so outcomes still land                                                                                                                                       |
| Vault secret `push_dispatch_secret` (must equal `PUSH_DISPATCH_SECRET` in `apps/people-web/.env.local`) next to `people_web_url`                                 | The `coupons_notify_followers` trigger actually calling `/api/push/new-coupon`                                                                   | Missing (needs the public URL first)                                                         | Call the endpoint by hand with the header (see verification-playbook); the trigger is a silent no-op meanwhile                                                                                                                  |
| An Android phone (Chrome) or an iPhone with the PWA installed, reachable over HTTPS                                                                              | Seeing a real notification arrive                                                                                                                | Not available in the in-app browser                                                          | Dispatch verified against fake endpoints (gone → deleted, unreachable → failed); production VAPID keys must be generated by the owner                                                                                           |
| Public tunnel to the dev machine (e.g. cloudflared/ngrok)                                                                                                        | Cloud Postgres calling `/api/billing/run` and `/api/push/new-coupon` on localhost; Wompi webhooks; camera/geolocation on phones (secure context) | Not set up                                                                                   | Unit-test handlers; tunnel only for the end-to-end run. The QR flow is verified on `localhost` through the manual-paste fallback on `/verificar`; a real camera scan between two phones is still pending                        |
| Wompi merchant settings: 3-D Secure not enforced, recurring/COF enabled                                                                                          | Payment sources come back `AVAILABLE` and charges succeed without the cardholder                                                                 | Sandbox behaves this way (verified)                                                          | `startTrialAction` shows "Tu banco requiere una verificación adicional…" when a source is `PENDING`                                                                                                                             |
| Contact handles (Instagram, TikTok, email; WhatsApp number)                                                                                                      | Phase 8 Contacto page                                                                                                                            | In place (2026-09-22) except WhatsApp                                                        | `CONTACT_LINKS` has Instagram `@discountmaps`, TikTok `@Discountmaps1`, `discountmaps1@gmail.com`; add a `whatsapp` entry (`https://wa.me/57…`) when a number exists                                                            |
| Brand assets: logo, icons 192/512/maskable, colors                                                                                                               | Final look of the icons, splash and `BrandMark`                                                                                                  | Missing                                                                                      | Generated icons (`scripts/generate-icons.mjs`, "%" map pin) ship in both `public/icons/`; drop the real PNGs in (same names) or edit the SVG in the script and re-run it. Tokens in `theme.css` are provisional                 |
| Vercel account, two hostnames, production Wompi credentials, Apple Developer account (post-launch)                                                               | Phase 10 deploy                                                                                                                                  | Missing                                                                                      | —                                                                                                                                                                                                                               |
| Docker Desktop on the dev machine                                                                                                                                | Local Supabase stack, `supabase test db`                                                                                                         | Not installed                                                                                | Linked cloud project + `scripts/pgtap-remote.mjs`                                                                                                                                                                               |

Open product questions still unanswered are listed in [mvp-plan.md § 9](mvp-plan.md).
