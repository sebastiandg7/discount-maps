# External dependencies and what they unblock

Inputs only the project owner can provide. Check here before starting a phase; work around a missing input where noted rather than blocking.

| Input                                                                                                                                                            | Unblocks                                                                                                                                         | Status (2026-09-21)                           | Workaround                                                                                                                                                                                               |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Google Maps API key (Maps JavaScript, Places New, Geocoding) + Map ID → `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`, `NEXT_PUBLIC_GOOGLE_MAPS_MAP_ID` in both `.env.local` | Map canvas + markers on `/mapas` and `/negocios/[id]`; Places autocomplete for branch addresses                                                  | **Missing** (2026-09-22)                      | Shipped without it: nearby list, chips, sort, geolocation fallback, profile, deep links. `BusinessMap` renders "Mapa no disponible"; swap its body for `@vis.gl/react-google-maps` when the key lands    |
| Google OAuth client configured in Supabase Auth (same Google Cloud project)                                                                                      | "Continuar con Google" on people-web                                                                                                             | Missing                                       | Email/password works                                                                                                                                                                                     |
| Wompi sandbox keys (`pub_test_`, `prv_test_`, `test_events_`, `test_integrity_`) + subscription price in COP                                                     | Phase 6 card capture, trial gate, billing cron, webhook                                                                                          | Missing                                       | Build the abstraction and tests against documented payloads; sandbox run last                                                                                                                            |
| `SUPABASE_SECRET_KEY` pasted into `apps/people-web/.env.local` by the owner                                                                                      | Any server code using `createAdminSupabase()` (billing, webhooks, push dispatch)                                                                 | Missing                                       | Not needed until Phase 6                                                                                                                                                                                 |
| Public tunnel to the dev machine (e.g. cloudflared/ngrok)                                                                                                        | Cloud Postgres calling `/api/billing/run` and `/api/push/new-coupon` on localhost; Wompi webhooks; camera/geolocation on phones (secure context) | Not set up                                    | Unit-test handlers; tunnel only for the end-to-end run. The QR flow is verified on `localhost` through the manual-paste fallback on `/verificar`; a real camera scan between two phones is still pending |
| Contact handles (WhatsApp number, Instagram, TikTok, email)                                                                                                      | Phase 8 Contacto page                                                                                                                            | Placeholders in `@org/domain` `CONTACT_LINKS` | Ship placeholders, swap later                                                                                                                                                                            |
| Brand assets: logo, icons 192/512/maskable, colors                                                                                                               | Phase 9 manifest, splash, install prompt                                                                                                         | Missing                                       | Tokens in `theme.css` are provisional                                                                                                                                                                    |
| Vercel account, two hostnames, production Wompi credentials, Apple Developer account (post-launch)                                                               | Phase 10 deploy                                                                                                                                  | Missing                                       | —                                                                                                                                                                                                        |
| Docker Desktop on the dev machine                                                                                                                                | Local Supabase stack, `supabase test db`                                                                                                         | Not installed                                 | Linked cloud project + `scripts/pgtap-remote.mjs`                                                                                                                                                        |

Open product questions still unanswered are listed in [mvp-plan.md § 9](mvp-plan.md).
